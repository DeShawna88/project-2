<%- include('../partials/html-head') %>
    <%- include('../partials/nav') %>
        <h1>Your Dessert Recipes</h1>

            <% recipes.forEach((recipe)=> { %>
                <a class="recipe" href="/desserts/<%= recipe._id %>">
                    <p>
                        <%= recipe.name %>
                    </p>
                </a>

                <% }) %>
        </div>
        </body>

        </html>